<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.HYBEF"
        tools:targetApi="31">
        <activity android:name=".artist.NewjeansActivity" />
        <activity android:name=".artist.LessActivity" />
        <activity android:name=".artist.FromisActivity" />
        <activity android:name=".artist.MinhyunActivity" />
        <activity android:name=".artist.BaekhoActivity" />
        <activity android:name=".artist.EnhypenActivity" />
        <activity android:name=".artist.SvtActivity" />
        <activity android:name=".artist.TxtActivity" />
        <activity android:name=".artist.BtsActivity" />
        <activity android:name=".auth.SignActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>